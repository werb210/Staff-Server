{
  "name": "staff-server-root",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix server install",
    "build": "npm --prefix server run build",
    "start": "node server/dist/index.js"
  }
}
