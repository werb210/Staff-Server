{
  "name": "staff-server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p server/tsconfig.json",
    "start": "node server/dist/index.js"
  },
  "engines": {
    "node": ">=20"
  }
}
